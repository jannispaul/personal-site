---
layout: "default-layout.njk"
---
{# <div class="flex"> #}
<div class="introduction">
    <h1 class="title">Jannis Wicke</h1>
    <p>Hi! Ich bin Jannis. Designer,  Entwickler und Mitgründer von <a href="https//:pixel-pink.de">PixelPink</a>. Wenn Du eine Frage hast oder mit mir arbeiten willst schreib mir eine <a href="mailto:jannis@pixel-pink.de">E-Mail</a>.
    </p>
</div>
<div class="stage" >
    {% for project in projects%}
        <iframe src={{project.url}} data-project data-{{project.data}} class="hidden"></iframe>
    {% endfor %}
</div>
{# </div> #}

<h2 class="section-headline">Projekte</h2>
<ul class="projects">
    {% for project in projects%}
        <li class="project" data-target={{project.data}}>
            <div>
                <span class="project-title">
                    {{project.title}}
                </span>
                <a href={{project.url}} data-{{project.data}} data-project  class="hidden">öffnen</a>
            </div>
        </li>
    {% endfor %}

</ul>